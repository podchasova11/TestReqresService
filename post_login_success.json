{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "token": {
      "type": "string",
      "minLength": 1  // Указывает, что токен не должен быть пустым
    }
  },
  "required": ["token"]
}
